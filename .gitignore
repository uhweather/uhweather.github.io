node_modules
dist
.DS_Store
*.local
.vite

# Working notes — kept locally, not part of the published repo.
PROGRESS.md
CLAUDE.md
.claude/

# Logo source files (.ai/.psd) — the exported SVG in public/ is what ships.
/logo/
